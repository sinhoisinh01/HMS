jQuery(document).ready(function(){
	setTimeout(function(){ 
		/*jQuery("#estimate-table, #prices-table, #analysis-table").DataTable( {
		    fixedHeader: true
		} );
		if(jQuery('#estimate-table').length > 0)
			jQuery('#estimate-table').remove();

		var _fixHeader ="<table id='estimate-table'><thead>"+jQuery(jQuery(jQuery('#estimate-table thead')).clone()).html()+"</thead></table>";
		jQuery(jQuery('#estimate-table').parent()).append(_fixHeader);
		jQuery('#estimate-table').addClass(jQuery('#estimate-table').attr("class"));

		console.log(jQuery('#estimate-table'));
		var top = jQuery('#estimate-table').offset().top;
		var left = jQuery('#estimate-table').offset().left
		jQuery('table#'+id).css({"max-width":jQuery(rootTable).width(),"position":"fixed","top":top,"z-index":"1000","left":left,"background-color":"white"});
		*/
	}, 3000);
});
